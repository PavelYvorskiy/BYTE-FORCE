VanillaTilt.init(document.querySelectorAll(".cardGlass"), {
  max: 0,
  speed: 3000,
  glare: true,
  "max-glare": 1,
});

VanillaTilt.init(document.querySelectorAll(".serviseCard"), {
  max: 5,
  speed: 3000,
  glare: true,
  "max-glare": 3,
});